app_identifier(ENV.fetch("APP_IDENTIFIER", "com.example.emochessApp"))
team_id(ENV.fetch("APPLE_TEAM_ID", "S3DSU79C4X"))

itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"]) if ENV["APP_STORE_CONNECT_TEAM_ID"] && !ENV["APP_STORE_CONNECT_TEAM_ID"].empty?
apple_id(ENV["APP_STORE_APPLE_ID"]) if ENV["APP_STORE_APPLE_ID"] && !ENV["APP_STORE_APPLE_ID"].empty?
